type Query {
    bookByTitle(title: String): Book
}

type Book {
    id: ID
    title: String
    author: String
    price: Int,
    cover: String
    publisher: String
    autorDescription: String
    bookDescription: String
    isbn: String
    stock: Int
}
